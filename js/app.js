
// GLOBAL VARIABLES
// Define the Object groundGrid with all its values ​​and possibilities
let groundGrid = {
    horizontalLength: 10,
    verticalLength: 10
};

// Define the Object rover with all its values ​​and possibilities
let rover = {
    direction: "N",
    positionX: 0,
    positionY: 0
};
// Define the Array directions with all possibilities
let directions = ["N", "E", "S", "W"];

